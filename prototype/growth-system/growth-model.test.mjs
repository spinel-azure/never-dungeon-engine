import assert from "node:assert/strict"; import { JOBS, calculateStats, getDeckCostAtLevel, getExperienceForLevel, getLevelGrowth, previewStatChange } from "./growth-model.js";
for (const job of Object.values(JOBS)) { assert.equal(Object.values(job.stats).reduce((sum,value)=>sum+value,0),24); assert.equal(job.hp+job.sp,45); }
assert.equal(getDeckCostAtLevel(1),3); assert.equal(getDeckCostAtLevel(5),6); assert.equal(getDeckCostAtLevel(197),48); assert.equal(getLevelGrowth("warrior",1).hp,30); assert.equal(getExperienceForLevel(197),9_999_999); assert.equal(calculateStats({jobId:"warrior",equipment:{str:2},cards:{str:3}}).stats.str.total,13); assert.equal(previewStatChange({jobId:"warrior",equipment:{str:20},cards:{}},"cards",{str:3}).allowed,false); assert.equal(previewStatChange({jobId:"warrior",equipment:{str:19},cards:{}},"cards",{str:3}).allowed,true); console.log("growth-model: all tests passed");
